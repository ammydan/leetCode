/***
 * Definition:
 * T is either empty
 * or consists of a node r(the root) and a possibly'
 * empty set of trees whose roots are the childen of r.
 *
 * Terminology:
 * siblings: two nodes that have the same parent are called siblings.
 * internal nodes:nodes that have children
 * external nodes or leaves: nodes that have children.
 *
 * ***/
public class BinaryTree {
    private Class Node<>{

    }
    private
}
